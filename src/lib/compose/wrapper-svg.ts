/**
 * @deprecated Import from `@/lib/compose/engine` instead.
 * Re-export keeps older call sites compiling during the compose migration.
 */
export {
  composeCreative,
  composeWrapperSvg,
  sizeForAspect,
  safeZoneForLayout,
  type ComposeInput,
  type BrandPalette,
} from "@/lib/compose/engine";
